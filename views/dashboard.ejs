<!DOCTYPE html>
<html>
<head>
  <meta charset="utf-8" />
  <title>Dashboard</title>
</head>
<body>
  <h1>Welcome <%= user ? user.name : 'User' %></h1>
  <% if (user && user.picture) { %>
    <img src="<%= user.picture %>" alt="Profile" style="width:120px;height:120px;border-radius:60px;">
  <% } %>
  <p>This is a simple dashboard placeholder for Google login callback.</p>
</body>
</html>
